# Config encrypt key
encrypt:
  key: test-encrypt-spring-cloud

server:
  port: 8080

# Application config
spring:
  application:
    name: licensingservice
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888

# Config eureka discovery
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/eureka/
